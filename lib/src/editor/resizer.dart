import 'package:flutter/widgets.dart';

(Offset positionDelta, Size sizeDelta, Offset shearDelta) resizeObject({
  required Matrix4 transform,
  required Offset resizeDelta,
  required Alignment resizeOrigin,
}) {
  // TODO
  return (Offset.zero, Size.zero, Offset.zero);
}
